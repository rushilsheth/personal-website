---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import BlogCard from "../components/BlogCard.astro";

import { getCollection } from "astro:content";
const posts = (await getCollection("blog"))
  .filter((p) => (!p.data.draft ?? false) || import.meta.env.MODE === "development")
  .sort((a, b) => a.data.pubDate.valueOf() - b.data.pubDate.valueOf())
  .reverse()
  .slice(0, 3);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<h1 class="page-title">🤝 Welcome!</h1>
			<p>

			👋 I’m Rushil, a passionate Machine Learning Engineer, outdoor enthusiast, and a devoted dog lover. Originally from St. Louis, Missouri (Go Cardinals!), I've been embracing the vibrant life and opportunities in the Bay Area for nearly a decade. Here in the Bay, I've found a perfect blend of intellectual stimulation, breathtaking nature, and culinary delights that keep me inspired every day.
			<br />
			At EchoAI, my role involves leveraging the transformative power of LLMs to discover hidden insights in customer conversations and amplify customers' voices across a company. Beyond work, I'm constantly exploring the potential of GenAI. My adventures in AI are not just about pushing boundaries in technology, but also about finding harmony between our digital and natural worlds.
			</p>
			<p>
				<h3>Recent Interests:</h3>
				<li>🚀 RAG - search engines are not outdated 😉</li>
				<li>? LLM Eval - big trust</li>
				<li>📝 Fine Tuned Models for situations and the everyday person</li>
				<li>⚡️ GenAI and productivity boosts</li>
				<br />
				Want to get in touch or have some reading material to suggest? Drop me a line at <a href="mailto:sheth.rushil@gmail.com">sheth.rushil@gmail.com</a></p>
			</p>
			
			<h2>From the blog</h2>
			<div
      			class="scrollbar-hidden flex max-w-full snap-x snap-mandatory scroll-px-6 gap-4 overflow-x-scroll py-8 md:snap-none">
      {
        posts.map(({ slug, data }) => (
          <BlogCard
            slug={slug}
            title={data.title}
            heroImage={data.heroImage}
            description={data.description}
            pubDate={data.pubDate}
            class="w-full min-w-[300px] grow snap-start md:min-w-0"
          />
        ))
      }
    </div>
			
			Blog stuff
			- autograders
			- beer week talk
			- busco-fin
			- "personal model"
		</main>
		<Footer />
	</body>
</html>
